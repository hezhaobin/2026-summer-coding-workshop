# Instructor analysis

Instructor-only exploratory analysis, quality-control investigations, model-development notes, and feasibility checks.

This directory is not the home for student notebooks, canonical datasets, or reusable pipeline code. Promote reusable logic to `../script/`, reusable validated tables to `../input/processed/`, final teaching notebooks to `../notebooks/`, and regenerable reports or figures to `../output/`.
